import { Given, Then, When } from '@cucumber/cucumber';

// Editor-only glue for the Cucumber VS Code language server.
// Runtime step definitions stay in features/step-definitions and use Step(...).

Given('kullanici login ekranini acar', () => undefined);

When('Kullanici gecerli bilgilerle giris yapar', () => undefined);

Then('kullanici basarili sekilde login olur', () => undefined);
